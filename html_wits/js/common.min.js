"use strict";

(function () {
  // // svg preload
  // let svg = document.querySelector('#mini-logo');
  // let svgAnimation = new LazyLinePainter(svg, {
  // 	strokeColor: '#000',
  // 	strokeWidth : 2,
  // 	strokeCap: 'square'
  // });
  //
  // svgAnimation.paint();
  // preload
  window.addEventListener('load', function () {
    var wrap = document.getElementsByClassName('preload-wrapper')[0];
    if (wrap.classList.contains('load')) return;
    setTimeout(function () {
      return wrap.classList.add('load');
    }, 3100);
  }); // NEXT PAGE

  var btnPage = document.querySelector('#js-next-page');
  var overlayPage = document.querySelector('.js-overlay-page');

  var _body = document.querySelector('.body');

  var _header = document.querySelector('.header');

  var words = document.querySelector('.js-words');
  var contactUs = document.querySelector('.js-contact-us');
  var homeFooter = document.querySelector('.js-home-footer');
  var page_2 = document.querySelector('.js-page');
  btnPage.addEventListener('click', function () {
    overlayPage.classList.add('active');
    setTimeout(function () {
      _body.classList.add('body_white');

      _header.classList.add('header_white');

      page_2.style.display = 'block';
    }, 1000);
    words.classList.add('d-none');
    homeFooter.classList.add('d-none');
    contactUs.classList.add('d-none');
  });
  $('#js-info').popover({
    html: true,
    container: 'body',
    template: '<div class="popover popover-custom">' + '<div class="arrow">' + '</div><h3 class="popover-header"></h3>' + '<div class="popover-body"></div>' + '</div>'
  });
})();